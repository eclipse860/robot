[project]
name = "remote-robot"
version = "0.5.0"
description = "Raspberry Pi robot control scripts and WebSocket command server."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
rpi = [
  "adafruit-blinka",
  "adafruit-circuitpython-pca9685",
  "websockets",
]
pc = [
  "websockets",
]
